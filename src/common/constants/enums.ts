export enum Order {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SortEnum {
  ASC = 'asc',
  DESC = 'desc',
}

export enum SubscriptionStatus {
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED',
  CANCELED = 'CANCELED',
}
